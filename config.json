{
  "mandrill_api_key": {
    "type": "string",
    "default": "",
    "description": "Mandrill (Mailchimp Transactional) API key"
  },
  "from_email": {
    "type": "string",
    "default": "noreply@example.com",
    "description": "From address used when sending via Mandrill"
  },
  "from_name": {
    "type": "string",
    "default": "VBWD",
    "description": "From display name used when sending via Mandrill"
  }
}
